server:
  port: 9001

spring:
  application:
    name: nacos-payment-provider #配置注册的服务名称
  cloud:
    nacos:
      discovery:
        server-addr: localhost:8848 #配置Nacos地址

# 暴露端口号
management:
  endpoints:
    web:
      exposure:
        include: '*'
